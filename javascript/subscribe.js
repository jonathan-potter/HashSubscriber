'use strict';

let subscribedProperties = {};

let subscriptions = {};

export default (propertyList, callback) => {
  /* make a subscription */

  /* add the subscription to the subscriptions object */

  /* add references to the subscription to each of the */
  /* subscribed properties */
};
